name: 数据采集 + 部署
# 由 cron-job.org 通过 repository_dispatch 触发，不用 GitHub 自带 schedule

on:
  repository_dispatch:
    types: [crawl]
  workflow_dispatch:  # 也允许手动触发
  push:
    branches: [main]

# 确保并发运行不会被互相覆盖
concurrency:
  group: pages-${{ github.ref }}
  cancel-in-progress: true

jobs:
  crawl:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
          cache-dependency-path: jiarenmens/requirements.txt

      - name: 安装依赖
        run: pip install requests aiohttp

      # 格局推送搭 crawl 便车(每日12次 dispatch), 时间窗各只触发一次:
      # 午盘=13:05 那次 / 尾盘=14:40 那次; push 触发不推
      - name: 午盘格局推送
        if: github.event_name == 'repository_dispatch'
        continue-on-error: true
        env:
          DINGTALK_URL: ${{ secrets.DINGTALK_URL }}
          DINGTALK_SECRET: ${{ secrets.DINGTALK_SECRET }}
        run: |
          cd jiarenmens
          H=$(TZ=Asia/Shanghai date +%H%M)
          if [ "$H" -ge 1300 ] && [ "$H" -le 1320 ]; then
            python scripts/cycle_push.py --session midday
          else
            echo "非午盘时段($H), 跳过"
          fi

      - name: 尾盘格局推送
        if: github.event_name == 'repository_dispatch'
        continue-on-error: true
        env:
          DINGTALK_URL: ${{ secrets.DINGTALK_URL }}
          DINGTALK_SECRET: ${{ secrets.DINGTALK_SECRET }}
        run: |
          cd jiarenmens
          H=$(TZ=Asia/Shanghai date +%H%M)
          if [ "$H" -ge 1430 ] && [ "$H" -le 1455 ]; then
            python scripts/cycle_push.py --session eod
          else
            echo "非尾盘时段($H), 跳过"
          fi

      # 收盘后(≥15:10)的 run: 先从 Release 热层恢复 db 再采集, 保证 40 日滚动窗口增量连续。
      # 白天 run 跳过(当天状态不持久化, 由收盘 run 全量重采覆盖, 见提交数据步骤注释)。
      - name: 下载热层库
        run: |
          H=$(TZ=Asia/Shanghai date +%H%M)
          if [ "$H" -lt 1510 ]; then echo "白天 run: 跳过热层下载"; exit 0; fi
          cd jiarenmens
          if python3 scripts/release_db.py --download-latest; then
            echo "✅ 已恢复热层"
          elif [ -f data/crawl_data.db ]; then
            echo "⚠️ 热层不存在(迁移过渡期), 使用 git 内 db 继续"
          else
            echo "❌ 热层下载失败且本地无 db, 终止以防数据断链"; exit 1
          fi

      - name: 数据采集
        run: cd jiarenmens && python main.py --checkpoint-reset

      # 东财人气榜快照(每天最多两份: 09:25 auction 已存 am 则此处自动记 pm, 同(date,snap)去重)
      - name: 人气榜快照
        if: success()
        continue-on-error: true
        run: cd jiarenmens && python scripts/auction_scan.py --hot-rank

      - name: 导出 JSON
        run: cd jiarenmens && python scripts/export_json.py --out ../stockboard-app/public/data

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: stockboard-app/package-lock.json

      - name: Build Vue
        run: cd stockboard-app && npm ci && npm run build

      # 钉钉通知放在「提交数据」之前：notify_daily.py 会写 last_notify_state.json，
      # 让状态文件跟随当次「提交数据」一起 commit 持久化，供下次对比"新增操作"
      - name: 钉钉通知
        if: success()
        continue-on-error: true
        env:
          DINGTALK_URL: ${{ secrets.DINGTALK_URL }}
          DINGTALK_SECRET: ${{ secrets.DINGTALK_SECRET }}
        run: cd jiarenmens && python3 scripts/notify_daily.py

      # 保留窗口外采集日删除+VACUUM, 封顶 crawl_data.db 体积(不清理则每天 +2~3MB 无限增长)
      - name: 清理过期采集数据
        run: cd jiarenmens && python3 scripts/prune_crawl_db.py --apply

      # 收盘后: 快照→热层覆盖 + 当周→温层 + 已完成月→冷层 + 温层滚动清理。
      # 上传失败立即终止并告警——宁可停, 不可断链(明天恢复时热层还是今天的完整状态)。
      - name: 上传 Release 存储(热/温/冷层)
        if: success()
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          H=$(TZ=Asia/Shanghai date +%H%M)
          if [ "$H" -lt 1510 ]; then echo "白天 run: 跳过上传"; exit 0; fi
          cd jiarenmens && python3 scripts/release_db.py --sync

      - name: 提交数据
        run: |
          git config user.name "bot"
          git config user.email "bot@users.noreply.github.com"
          git add -f stockboard-app/public/data/ jiarenmens/data/
          # crawl_data.db 已迁往 Release 三层存储(db-state/db-w*/db-m*), git 永不提交:
          # 二进制快照进 git 是仓库膨胀主因(.git 曾 721MB), 历史取数改用 scripts/fetch_db.py。
          # 白天不持久化 db 也安全: trades/positions 按 (zh_id,crawl_date) 先删后写,
          # 收盘 run 会全量重采当日数据(经热层恢复后增量入库)。
          git reset -q jiarenmens/data/crawl_data.db 2>/dev/null || true
          if git diff --staged --quiet; then
            echo "数据无变化，跳过提交"
          else
            git commit -m "📊 数据更新 $(date +%Y-%m-%d) [skip ci]"
            # 先拉取再推送，避免冲突；失败重试 3 次
            for i in 1 2 3; do
              if git pull --rebase && git push; then
                echo "✅ 推送成功"
                exit 0
              fi
              echo "⚠️ 推送失败，重试 $i/3..."
              sleep 5
            done
            echo "❌ 推送失败"; exit 1
          fi

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: stockboard-app/dist

  deploy:
    needs: crawl
    runs-on: ubuntu-latest
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
