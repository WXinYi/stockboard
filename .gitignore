# Dependencies
node_modules/
dist/
dist-ssr

# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.eggs/

# Virtual environments
venv/
.venv/
.venv

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Logs
logs/
*.log

# uv
uv.lock

# Env
.env
.env.local
.env.*.local

# Visual companion mockups
.superpowers/

# Project doc (local only)
CLAUDE.md
*.db-shm
*.db-wal

# 盘中监控(本机独享, 不提交)
jiarenmens/data/intraday.db
jiarenmens/data/analysis.db
jiarenmens/data/intraday_page.html
jiarenmens/data/watchlist_extra.txt

# 会话/误写产物, 不入库
.zcode/
# 安全钩子(Mimosa)的会话状态与源码基线副本: 每轮都往这里写一份影子文件,
# 2026-09-15 实测单个会话就 339 个文件 ≈ 7.4 万行, 会把"待提交"数字撑到几万行
.mimosa/
jiarenmens/jiarenmens/

# 数据库已迁往 Release 三层存储(db-state/db-w*/db-m*), git 不再跟踪(见 docs/DATA_PIPELINE.md)
jiarenmens/data/crawl_data.db

# auction.db 同迁 Release 热层(auction-state, 每班 sha 门下载/上传), git 不再跟踪
jiarenmens/data/auction.db

# 回测归档(fetch_db 产物), 不入库
jiarenmens/data/archive/

# ④降级链产物(release_db --download-fallback / crawl.yml cache 副本)
jiarenmens/data/.db_restore_source
jiarenmens/data/.cache-db/

# 盯盘 watchdog 的运行时状态(进 actions/cache, 不进 git)
jiarenmens/data/.watchdog_state.json
jiarenmens/data/.watchdog_changed
